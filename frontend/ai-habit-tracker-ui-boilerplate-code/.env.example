# Local development
VITE_API_URL=http://localhost:8000/api

# Production (Vercel) — set in Vercel → Project → Settings → Environment Variables
# Use your Render backend URL (no trailing slash before /api):
# VITE_API_URL=https://ai-habit-tracker-api.onrender.com/api
